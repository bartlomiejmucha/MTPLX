# MTPLX 2.11.3

A quality release. Three independent audits of the decode path (the reports behind "the 8-bit Quality pack loops too" and "the answers are worse than the competition") were reconciled against the code and the machine, and every defect they agreed on is fixed here with a test that pins the law: the Flash-Next block-verify acceptance law is exact at every depth, warm agent turns on the hybrid 27B models restore the recurrent state they were banked with, the state banked after a speculative turn covers every committed token, non-finite logits fail loudly instead of printing exclamation marks, the Flash-Next AR sampler follows the reference nucleus law, and Hindi, Thai and vowelled Arabic tokenize as the models were trained. None of it costs speed. On the same Mac, runtime, prompt and sampler, Flash-Next decodes 11.7 percent faster than 2.11.2 (82.1 to 91.7 tok/s at 8.8k tokens of context on an M5 Max, identical acceptance by depth), because four serving optimizations a served daemon never engaged now do, and the exact block-verify law costs nothing measurable on top. Around that core: 261k-token prompts decode on Flash-Next (PR #482), 32 GB Macs get a working draft head (#483), OpenCode stops compacting after every reply on small windows (#480), and the four Flash-Next serving gates that a served daemon never engaged are engaged.

## Why the Quality pack looped: five exactness defects, none of them the weights

The reports pointed at the 8-bit weights. The weights are fine; the drift was in the machinery around them, and each piece was reproduced on tiny cases before it was fixed.

- **Block verification at depth 3 was not exact.** Enumerating the accept law on a small vocabulary shows it exact at depths 1 and 2 and off by up to 4e-2 total variation at depth 3, on windows where the last depth's coin had been clipped to 1. The ladder now caps the reach budget by the realised reach, propagates that feasible budget, and corrects a rejection from the deficit the coins leave. An enumeration test compares the emitted joint law with the target for both laws at depths 1 to 4 (exact to 1e-12). On the shipped daemon this lane was in fact never engaged (see the gates below), so the fix is what makes it safe to engage.
- **A warm agent turn restored the wrong recurrent state on the hybrid models.** A near-prefix restore whose gap to the banked turn was 1 to 8 tokens, the shape of a re-rendered agent turn, trimmed the attention KV but kept the GDN state from the longer sequence, then re-ran one token on it. The whole turn decoded on state the new prompt never produced and later turns inherited it, on the 8-bit Quality pack exactly like the 4-bit ones. Every partial restore of a recurrent entry now lands on a stored recurrent boundary at or below the match point.
- **The banked state after a speculative turn was one token short.** The primary that ended a response (a deferred greedy correction, a fresh sample, or any max_tokens exit) was committed but never forwarded, so the next warm turn decoded as if the terminator never existed. Temperature-0 clients such as Cline hit it every turn. The ending primary is forwarded before the state is banked.
- **Non-finite logits became token 0**, which is `!` in the Qwen vocabulary (the thousands of exclamation marks of #311). The request now ends with `finish_reason: error`, code `non_finite_logits`, a message with the NaN and inf counts, the session's cached state is dropped, and the daemon stays up.

Two more exactness repairs sit beside them: the pipelined Flash-Next AR sampler measured top-p on the top-k slice alone, keeping one to five fewer of the top-20 tokens than every other lane at 1.0/0.95/20, and now measures the nucleus on the full-vocabulary softmax like the CPU reference; and the loader restores the Qwen3 pre-tokenizer regex on every Qwen3-family pack, because transformers' Qwen2Tokenizer had replaced it with its Qwen2-era one and split combining marks off their letters (Hindi 32 tokens instead of 20, Thai 17 instead of 9, Arabic 45 instead of 31 on the same sentences; Latin, code and Chinese unchanged). The six 27B packs on the Hub ship that regex baked into tokenizer.json; the loader repair covers them until the files are re-pushed.

### The fifth: a copied block at the cache's growth edge lost its rows

The context-copy lane proposes a block of up to 24 tokens copied verbatim from the prompt and verifies it in one forward. On the 27B family that forward writes its key/value rows into the fixed-capacity buffers the compiled verify installs, and those buffers grow in 512-token grants. A copy block that straddled a grant edge was written with the functional slice update, which on Metal silently drops the rows past the end while the offset still advances over them, so the rest of the turn attended over missing rows. Reproduced with a six-turn coding session on the Optimized Quality pack: 2.11.2's fourth answer agrees with the fixed engine's for 3,001 characters and then ends the turn in the middle of `parser.add_argument(`, where the fixed engine goes on to write the option and the subcommands. With reasoning on, the same turn from the same restored point and seed parts from the fixed engine inside the imports copied from context and then writes the complete `cli.py` twice, verbatim, to the token cap; the fixed engine writes it once and stops. The copy-block route now grows every fixed buffer before the forward, and the write itself grows the buffer as a last guard; the request log counts the growths under `ccopy_capacity_growths`. In-tree since the copy lane and the fixed verify buffers first met (2.10.0).

## The Flash-Next serving gates reach a served daemon

Four hot-path gates (`MTPLX_QWEN4_OPDIET`, `MTPLX_QWEN4_VERIFY_GLUE`, `MTPLX_QWEN4_DRAFT_K20_PRESCATTER`, `MTPLX_QWEN4_BLOCK_VERIFY`) were read once at import, before the server stamped the Flash-Next lane defaults, so `/health` reported them configured while the daemon ran with all four off. David Tai's PR #475 found the same frozen readers. The gates are re-read when the model's runtime env is applied, before the load, and `/health` now shows every lane key with `observed` equal to `expected` on a served daemon. With the exact block-verify law in place the block-verify lane costs nothing measurable: off 91.65 / 97.92 / 93.34 tok/s, on 91.44 / 97.26 / 92.01 tok/s across alternating boots on the same 8,848-token prompt, acceptance by depth 407/364/323 against 408/361/324 of 441 drafts. Measured against 2.11.2 on the same Python runtime, same Mac (M5 Max), same 8,848-token prompt and sampler, alternating boots with the fans at maximum: 82.1 tok/s on 2.11.2, 91.7 tok/s on this release, +11.7 percent, with identical acceptance by depth.

## Flash-Next at full context (PR #482, davidtai)

At the pack's 261,120-token context the first speculative verify step ran out of GPU memory after the whole prompt had been read: the verify's KV-cache update reallocated the entire cache buffer on every step (about 6.4 GB across the twelve full-attention layers at that context), and its multi-row attention built a score table that grows with the context. The KV update now writes its rows into the existing buffer, and the verify attention is split across query-head groups so each call stays on the fused kernel in the three-to-eight-row band the verify uses. Both are exact. `MTPLX_QWEN4_VERIFY_SDPA_HEAD_CHUNK=0` turns the split off and `/health` reports `verify_sdpa_head_chunk` when it engages. Speed and peak memory at 16k are unchanged; the 27B models are not affected.

## 32 GB Macs: the draft head is built without the dense intermediate (#483)

The fast draft LM head was built by dequantizing the whole 8-bit head into one 2.4 GiB dense tensor and requantizing that in one shot. On an M1 Pro 32 GB the requantize hit a silent Metal out-of-memory and the head read back as zeros, collapsing MTP acceptance to under 1% and failing the tuner in its first warmup. The head is now built in row chunks (bit-identical result, no dense intermediate), a zeroed head is refused, and the install falls back to the resident target head as the drafter, which the reporter's own measurements show is equivalent in quality. The startup report records which path was taken.

## Long agent turns stay warm across the model's own token seams

A model's sampled tokens are not always the canonical encoding of their own text. In a Hermes session at effort xhigh the 27B wrote `"Nothing` as a single token 8,498 tokens into a write_file call, where the tokenizer encodes `"` and then `Nothing`. The bytes were identical, the ids were not, and everything downstream treated that as an edit: the re-tokenized history could never match the committed stream, the end-of-turn snapshot was refused, the fallback re-prefill lost the race against Hermes' next request, and that request re-prefilled the whole 29,842-token turn (41 seconds to the first token). Wherever the resent prompt and the session's committed ids decode to the same text, the committed ids are now served, both when a request is matched to its session and when a finished turn is banked. A real edit still ends the match where it happens. The same splice puts back the whitespace a reply's visible content is served without (a turn cut by the token budget right after a newline used to come back with the end-of-turn marker where the newline was, and the exactness rule then re-prefilled the whole turn on the next request), and it runs whether or not thinking is on. The request log records the splice under `committed_reasoning_canonicalization.token_splice`; `MTPLX_COMMITTED_TOKEN_SPLICE=0` turns it off.

## Agent harnesses

- **OpenCode compacted after every reply on small context windows (#480).** MTPLX registered the model with the reply limit equal to the context window; OpenCode reserves the reply limit out of the window before deciding whether the conversation still fits, so on an 8K window the usable conversation was zero tokens and OpenCode wrote its `## Objective / Important Details / Work State` summary after every turn (48 summaries in 98 turns in the report). `mtplx connect opencode`, the quickstart and the app now advertise a reply budget of half the window, capped at the 32,000 OpenCode uses on large windows.
- **A mid-conversation system message no longer rewrites message 0 (#477).** Claude Code's per-turn `<system-reminder>` notes arrive as late system messages; hoisting them into the leading system message changed the first message every turn and cost a cold re-prefill of the whole history. A late system message now becomes a user turn in place.
- **Reasoning substitution checks the whole tool call.** The committed-reasoning canonicalizer compared tool calls by their loop key (command or path only), so a `write` to the same file with new content passed the gate and the prompt was served with the old body. The gate now compares the complete argument set in both markup dialects.
- **`mtplx connect opencode` follows the live daemon (#472)**, streams no longer hold for minutes when the first line looks like tool-control markup (#468), the Hermes profile warning on every launch is gone, and the app's Pi and Hermes launches on Flash-Next draft to the chosen depth instead of the expected-value policy (measured 7 to 8 percent slower than a fixed depth 3 on the shipped lane).

## Session cache

There is no five- or ten-minute timer on the warm cache (#481): the idle sweep is one hour, and the ten-minute window only orders eviction victims under pressure. What the report shows is a 2.10.2 cache miss that 2.11.2 already fixed. The idle limit is now a knob, `MTPLX_SESSION_BANK_IDLE_TTL_S` (`0` keeps entries until memory needs them), and the five `MTPLX_SESSION_BANK_*` variables are documented together in `docs/server.md` for the first time.

## Doctor, CORS, retrieval, dashboard

- `mtplx doctor` reports which MTPLX answers (`runtime.identity`: version, path, whether the `mtplx` first on PATH runs the same interpreter, GPU architecture, and whether the M5 tensor-unit route is available). The kernel-build failure of #479 on macOS 15 is unreachable on 2.11.2 code; the check answers which MTPLX actually ran.
- Desktop web views can be allowlisted for CORS (#473): any scheme is an origin scheme now, so `--cors-origin tauri://localhost` works for Jan.app; wildcards and the admin routes stay closed.
- Retrieval models are validated at startup (#445 audit); the dashboard request log prints real per-depth acceptance totals and completion times (#401); the request-capture registry no longer grows for the life of the daemon (PR #356); `/health` fast-path verdicts compare against the server's own overrides.
- Reasoning effort is documented (#484): `reasoning_effort` per request, `--reasoning-effort` on the server, the app's picker, and the per-family tiers.

## Also in this release

Turkish (#470), the Appearance row, chat sidebar timestamps, launch-chooser and Forge captions localized in all thirteen languages; removal of a stale custom model from the picker (PR #471, nRanzo); the aria2 download backend as an opt-in (PR #452, zeeshanhaque21); the README model table (#238, #408); the Responses API (Philip John Basile); the Flash-Next converter accepts the official FP8 checkpoint (PR #474, Graham Jenkins); transformers floor raised to 5.10.0; the flight recorder counts tokens for non-streaming requests (a 24k-token non-stream generation used to read as a prefill with no tokens for its whole life).

## Validated with the real harnesses

On the release branch, on an M5 Max with the fans at maximum and one resident model at a time: the full Python suite (6,797 passed, 35 skipped) and the app's OpenCode suite; OpenCode (flappy bird with a second turn, a screenshot task with a second turn), Pi (snake with tests, a screenshot task with a second turn) and Hermes (a todo CLI with 31 passing tests, a screenshot task), nine steps, every one exit code 0 against a Flash-Next daemon running this code. Speed against 2.11.2 on the same runtime, prompt and sampler, alternating boots: Flash-Next 82.1 to 91.7 tok/s (+11.7 percent), the 27B Quality pack 50.3 to 50.2 tok/s (a tie), identical acceptance on every arm. The 27B Quality pack's flappy output at temperature 1 with a fixed seed is byte-identical on 2.11.2 and this release; the same pack's warm second turn after a truncated answer differs from cold on 2.11.2 and matches byte for byte here. A candidate app build served the founder's chat prompt through its own daemon on this code.

## Upgrading

`brew upgrade mtplx`, `pip install -U mtplx`, or the app's own update check. No settings change. The six 27B packs on the Hub keep their tokenizer.json until they are re-pushed; the loader repair applies either way.
